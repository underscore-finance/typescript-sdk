/* Autogenerated file. Do not edit manually. */
/* tslint:disable */
/* eslint-disable */
/* @ts-nocheck */

import { AddressResolverFunction } from '@dappql/async'
import { PublicClient, WalletClient } from 'viem'

import * as AaveV3 from './AaveV3.js'
import * as AeroClassic from './AeroClassic.js'
import * as AeroSlipstream from './AeroSlipstream.js'
import * as AgentSenderGeneric from './AgentSenderGeneric.js'
import * as AgentSenderSpecial from './AgentSenderSpecial.js'
import * as AgentWrapper from './AgentWrapper.js'
import * as AgentWrapperV1 from './AgentWrapperV1.js'
import * as Appraiser from './Appraiser.js'
import * as Avantis from './Avantis.js'
import * as Billing from './Billing.js'
import * as ChequeBook from './ChequeBook.js'
import * as CompoundV3 from './CompoundV3.js'
import * as ConvertVaultToken from './ConvertVaultToken.js'
import * as Curve from './Curve.js'
import * as DefaultsBase from './DefaultsBase.js'
import * as ERC20 from './ERC20.js'
import * as EarnVault from './EarnVault.js'
import * as EarnVaultAgent from './EarnVaultAgent.js'
import * as EarnVaultAgentSigHelper from './EarnVaultAgentSigHelper.js'
import * as Euler from './Euler.js'
import * as ExtraFi from './ExtraFi.js'
import * as Fluid from './Fluid.js'
import * as FortyAcres from './FortyAcres.js'
import * as Hatchery from './Hatchery.js'
import * as Helpers from './Helpers.js'
import * as HighCommand from './HighCommand.js'
import * as Kernel from './Kernel.js'
import * as Ledger from './Ledger.js'
import * as LegoBook from './LegoBook.js'
import * as LegoTools from './LegoTools.js'
import * as LevgVault from './LevgVault.js'
import * as LevgVaultAgent from './LevgVaultAgent.js'
import * as LevgVaultAgentSigHelper from './LevgVaultAgentSigHelper.js'
import * as LevgVaultHelper from './LevgVaultHelper.js'
import * as LevgVaultTools from './LevgVaultTools.js'
import * as LootDistributor from './LootDistributor.js'
import * as Migrator from './Migrator.js'
import * as MissionControl from './MissionControl.js'
import * as Moonwell from './Moonwell.js'
import * as Morpho from './Morpho.js'
import * as Paymaster from './Paymaster.js'
import * as RipeLego from './RipeLego.js'
import * as Sentinel from './Sentinel.js'
import * as SignatureHelper from './SignatureHelper.js'
import * as SignatureHelperV1 from './SignatureHelperV1.js'
import * as SkyPsm from './SkyPsm.js'
import * as Switchboard from './Switchboard.js'
import * as SwitchboardAlpha from './SwitchboardAlpha.js'
import * as SwitchboardBravo from './SwitchboardBravo.js'
import * as SwitchboardCharlie from './SwitchboardCharlie.js'
import * as UnderscoreLego from './UnderscoreLego.js'
import * as UndyAero from './UndyAero.js'
import * as UndyBtc from './UndyBtc.js'
import * as UndyCbeth from './UndyCbeth.js'
import * as UndyEth from './UndyEth.js'
import * as UndyEurc from './UndyEurc.js'
import * as UndyGho from './UndyGho.js'
import * as UndyHq from './UndyHq.js'
import * as UndyRewardsLego from './UndyRewardsLego.js'
import * as UndyUsd from './UndyUsd.js'
import * as UndyUsds from './UndyUsds.js'
import * as UndyVirtual from './UndyVirtual.js'
import * as UniswapV2 from './UniswapV2.js'
import * as UniswapV3 from './UniswapV3.js'
import * as UserWallet from './UserWallet.js'
import * as UserWalletConfig from './UserWalletConfig.js'
import * as UserWalletConfigV1 from './UserWalletConfigV1.js'
import * as UserWalletSignatureHelper from './UserWalletSignatureHelper.js'
import * as UserWalletV1 from './UserWalletV1.js'
import * as VaultRegistry from './VaultRegistry.js'
import * as WalletBackpack from './WalletBackpack.js'
import * as Wasabi from './Wasabi.js'

export type SDK = {
  AaveV3: AaveV3.SDK
  AeroClassic: AeroClassic.SDK
  AeroSlipstream: AeroSlipstream.SDK
  AgentSenderGeneric: AgentSenderGeneric.SDK
  AgentSenderSpecial: AgentSenderSpecial.SDK
  AgentWrapper: AgentWrapper.SDK
  AgentWrapperV1: AgentWrapperV1.SDK
  Appraiser: Appraiser.SDK
  Avantis: Avantis.SDK
  Billing: Billing.SDK
  ChequeBook: ChequeBook.SDK
  CompoundV3: CompoundV3.SDK
  ConvertVaultToken: ConvertVaultToken.SDK
  Curve: Curve.SDK
  DefaultsBase: DefaultsBase.SDK
  ERC20: (address: `0x${string}`) => ERC20.SDK
  EarnVault: (address: `0x${string}`) => EarnVault.SDK
  EarnVaultAgent: EarnVaultAgent.SDK
  EarnVaultAgentSigHelper: EarnVaultAgentSigHelper.SDK
  Euler: Euler.SDK
  ExtraFi: ExtraFi.SDK
  Fluid: Fluid.SDK
  FortyAcres: FortyAcres.SDK
  Hatchery: Hatchery.SDK
  Helpers: Helpers.SDK
  HighCommand: HighCommand.SDK
  Kernel: Kernel.SDK
  Ledger: Ledger.SDK
  LegoBook: LegoBook.SDK
  LegoTools: LegoTools.SDK
  LevgVault: (address: `0x${string}`) => LevgVault.SDK
  LevgVaultAgent: LevgVaultAgent.SDK
  LevgVaultAgentSigHelper: LevgVaultAgentSigHelper.SDK
  LevgVaultHelper: (address: `0x${string}`) => LevgVaultHelper.SDK
  LevgVaultTools: LevgVaultTools.SDK
  LootDistributor: LootDistributor.SDK
  Migrator: Migrator.SDK
  MissionControl: MissionControl.SDK
  Moonwell: Moonwell.SDK
  Morpho: Morpho.SDK
  Paymaster: Paymaster.SDK
  RipeLego: RipeLego.SDK
  Sentinel: Sentinel.SDK
  SignatureHelper: SignatureHelper.SDK
  SignatureHelperV1: SignatureHelperV1.SDK
  SkyPsm: SkyPsm.SDK
  Switchboard: Switchboard.SDK
  SwitchboardAlpha: SwitchboardAlpha.SDK
  SwitchboardBravo: SwitchboardBravo.SDK
  SwitchboardCharlie: SwitchboardCharlie.SDK
  UnderscoreLego: UnderscoreLego.SDK
  UndyAero: UndyAero.SDK
  UndyBtc: UndyBtc.SDK
  UndyCbeth: UndyCbeth.SDK
  UndyEth: UndyEth.SDK
  UndyEurc: UndyEurc.SDK
  UndyGho: UndyGho.SDK
  UndyHq: UndyHq.SDK
  UndyRewardsLego: UndyRewardsLego.SDK
  UndyUsd: UndyUsd.SDK
  UndyUsds: UndyUsds.SDK
  UndyVirtual: UndyVirtual.SDK
  UniswapV2: UniswapV2.SDK
  UniswapV3: UniswapV3.SDK
  UserWallet: (address: `0x${string}`) => UserWallet.SDK
  UserWalletConfig: (address: `0x${string}`) => UserWalletConfig.SDK
  UserWalletConfigV1: (address: `0x${string}`) => UserWalletConfigV1.SDK
  UserWalletSignatureHelper: UserWalletSignatureHelper.SDK
  UserWalletV1: (address: `0x${string}`) => UserWalletV1.SDK
  VaultRegistry: VaultRegistry.SDK
  WalletBackpack: WalletBackpack.SDK
  Wasabi: Wasabi.SDK
}

export default function createSdk(
  publicClient?: PublicClient,
  walletClient?: WalletClient,
  addressResolver?: AddressResolverFunction,
): SDK {
  return {
    AaveV3: AaveV3.toSdk(publicClient, walletClient, addressResolver),
    AeroClassic: AeroClassic.toSdk(publicClient, walletClient, addressResolver),
    AeroSlipstream: AeroSlipstream.toSdk(publicClient, walletClient, addressResolver),
    AgentSenderGeneric: AgentSenderGeneric.toSdk(publicClient, walletClient, addressResolver),
    AgentSenderSpecial: AgentSenderSpecial.toSdk(publicClient, walletClient, addressResolver),
    AgentWrapper: AgentWrapper.toSdk(publicClient, walletClient, addressResolver),
    AgentWrapperV1: AgentWrapperV1.toSdk(publicClient, walletClient, addressResolver),
    Appraiser: Appraiser.toSdk(publicClient, walletClient, addressResolver),
    Avantis: Avantis.toSdk(publicClient, walletClient, addressResolver),
    Billing: Billing.toSdk(publicClient, walletClient, addressResolver),
    ChequeBook: ChequeBook.toSdk(publicClient, walletClient, addressResolver),
    CompoundV3: CompoundV3.toSdk(publicClient, walletClient, addressResolver),
    ConvertVaultToken: ConvertVaultToken.toSdk(publicClient, walletClient, addressResolver),
    Curve: Curve.toSdk(publicClient, walletClient, addressResolver),
    DefaultsBase: DefaultsBase.toSdk(publicClient, walletClient, addressResolver),
    ERC20: (address: `0x${string}`) => ERC20.toSdk(address, publicClient, walletClient, addressResolver),
    EarnVault: (address: `0x${string}`) => EarnVault.toSdk(address, publicClient, walletClient, addressResolver),
    EarnVaultAgent: EarnVaultAgent.toSdk(publicClient, walletClient, addressResolver),
    EarnVaultAgentSigHelper: EarnVaultAgentSigHelper.toSdk(publicClient, walletClient, addressResolver),
    Euler: Euler.toSdk(publicClient, walletClient, addressResolver),
    ExtraFi: ExtraFi.toSdk(publicClient, walletClient, addressResolver),
    Fluid: Fluid.toSdk(publicClient, walletClient, addressResolver),
    FortyAcres: FortyAcres.toSdk(publicClient, walletClient, addressResolver),
    Hatchery: Hatchery.toSdk(publicClient, walletClient, addressResolver),
    Helpers: Helpers.toSdk(publicClient, walletClient, addressResolver),
    HighCommand: HighCommand.toSdk(publicClient, walletClient, addressResolver),
    Kernel: Kernel.toSdk(publicClient, walletClient, addressResolver),
    Ledger: Ledger.toSdk(publicClient, walletClient, addressResolver),
    LegoBook: LegoBook.toSdk(publicClient, walletClient, addressResolver),
    LegoTools: LegoTools.toSdk(publicClient, walletClient, addressResolver),
    LevgVault: (address: `0x${string}`) => LevgVault.toSdk(address, publicClient, walletClient, addressResolver),
    LevgVaultAgent: LevgVaultAgent.toSdk(publicClient, walletClient, addressResolver),
    LevgVaultAgentSigHelper: LevgVaultAgentSigHelper.toSdk(publicClient, walletClient, addressResolver),
    LevgVaultHelper: (address: `0x${string}`) =>
      LevgVaultHelper.toSdk(address, publicClient, walletClient, addressResolver),
    LevgVaultTools: LevgVaultTools.toSdk(publicClient, walletClient, addressResolver),
    LootDistributor: LootDistributor.toSdk(publicClient, walletClient, addressResolver),
    Migrator: Migrator.toSdk(publicClient, walletClient, addressResolver),
    MissionControl: MissionControl.toSdk(publicClient, walletClient, addressResolver),
    Moonwell: Moonwell.toSdk(publicClient, walletClient, addressResolver),
    Morpho: Morpho.toSdk(publicClient, walletClient, addressResolver),
    Paymaster: Paymaster.toSdk(publicClient, walletClient, addressResolver),
    RipeLego: RipeLego.toSdk(publicClient, walletClient, addressResolver),
    Sentinel: Sentinel.toSdk(publicClient, walletClient, addressResolver),
    SignatureHelper: SignatureHelper.toSdk(publicClient, walletClient, addressResolver),
    SignatureHelperV1: SignatureHelperV1.toSdk(publicClient, walletClient, addressResolver),
    SkyPsm: SkyPsm.toSdk(publicClient, walletClient, addressResolver),
    Switchboard: Switchboard.toSdk(publicClient, walletClient, addressResolver),
    SwitchboardAlpha: SwitchboardAlpha.toSdk(publicClient, walletClient, addressResolver),
    SwitchboardBravo: SwitchboardBravo.toSdk(publicClient, walletClient, addressResolver),
    SwitchboardCharlie: SwitchboardCharlie.toSdk(publicClient, walletClient, addressResolver),
    UnderscoreLego: UnderscoreLego.toSdk(publicClient, walletClient, addressResolver),
    UndyAero: UndyAero.toSdk(publicClient, walletClient, addressResolver),
    UndyBtc: UndyBtc.toSdk(publicClient, walletClient, addressResolver),
    UndyCbeth: UndyCbeth.toSdk(publicClient, walletClient, addressResolver),
    UndyEth: UndyEth.toSdk(publicClient, walletClient, addressResolver),
    UndyEurc: UndyEurc.toSdk(publicClient, walletClient, addressResolver),
    UndyGho: UndyGho.toSdk(publicClient, walletClient, addressResolver),
    UndyHq: UndyHq.toSdk(publicClient, walletClient, addressResolver),
    UndyRewardsLego: UndyRewardsLego.toSdk(publicClient, walletClient, addressResolver),
    UndyUsd: UndyUsd.toSdk(publicClient, walletClient, addressResolver),
    UndyUsds: UndyUsds.toSdk(publicClient, walletClient, addressResolver),
    UndyVirtual: UndyVirtual.toSdk(publicClient, walletClient, addressResolver),
    UniswapV2: UniswapV2.toSdk(publicClient, walletClient, addressResolver),
    UniswapV3: UniswapV3.toSdk(publicClient, walletClient, addressResolver),
    UserWallet: (address: `0x${string}`) => UserWallet.toSdk(address, publicClient, walletClient, addressResolver),
    UserWalletConfig: (address: `0x${string}`) =>
      UserWalletConfig.toSdk(address, publicClient, walletClient, addressResolver),
    UserWalletConfigV1: (address: `0x${string}`) =>
      UserWalletConfigV1.toSdk(address, publicClient, walletClient, addressResolver),
    UserWalletSignatureHelper: UserWalletSignatureHelper.toSdk(publicClient, walletClient, addressResolver),
    UserWalletV1: (address: `0x${string}`) => UserWalletV1.toSdk(address, publicClient, walletClient, addressResolver),
    VaultRegistry: VaultRegistry.toSdk(publicClient, walletClient, addressResolver),
    WalletBackpack: WalletBackpack.toSdk(publicClient, walletClient, addressResolver),
    Wasabi: Wasabi.toSdk(publicClient, walletClient, addressResolver),
  }
}
